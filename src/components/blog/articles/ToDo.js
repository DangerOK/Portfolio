import React from "react";
import ToDoThumbnail from "../../../assets/images/resources/ToDoThumbnail.png";

const ToDo = () => (
  <div className="article-wrapper">
    <h1>To-Do App</h1>
    <img src={ToDoThumbnail} alt="site thumbnail" />
    <p>
      A classic. The first thing thing I built in React. A very simple Todo App.
    </p>
  </div>
);

export default ToDo;
